#include "main.h"

/**
 * mul - function that multiplies two integers
 * @a: first variable
 * @b: second variable
 * Return: Always (0)
 *
 */
int mul(int a, int b)
{
int mul;

mul = a * b;

return (mul);
}
