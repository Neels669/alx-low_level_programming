#include "main.h"

/**
 * _isupper - Function to check for an upper case
 * @c: integer to be checked
 *
 * Return: 1 if uppercase else 0
 */
int _isupper(int c)
{
	if (_isupper(c))
		return (1);
	else
		return (0);
}
